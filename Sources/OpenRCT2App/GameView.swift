import SwiftUI

struct GameView: View {
    var body: some View {
        VStack {
            Text("OpenRCT2 on visionOS")
                .font(.title)
            Text("Rendering pipeline to be implemented")
                .font(.caption)
        }
        .padding()
    }
}
