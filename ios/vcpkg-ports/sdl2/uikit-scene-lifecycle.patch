diff --git a/src/video/uikit/SDL_uikitappdelegate.h b/src/video/uikit/SDL_uikitappdelegate.h
index 501ca0fb7..0572536b6 100644
--- a/src/video/uikit/SDL_uikitappdelegate.h
+++ b/src/video/uikit/SDL_uikitappdelegate.h
@@ -29,6 +29,13 @@
 
 @end
 
+API_AVAILABLE(ios(13.0))
+@interface SDLUIKitSceneDelegate : NSObject<UIApplicationDelegate, UIWindowSceneDelegate>
+
++ (NSString *)getSceneDelegateClassName;
+
+@end
+
 @interface SDLUIKitDelegate : NSObject<UIApplicationDelegate>
 
 + (id)sharedAppDelegate;
diff --git a/src/video/uikit/SDL_uikitappdelegate.m b/src/video/uikit/SDL_uikitappdelegate.m
index 8863a678f..7374b73b8 100644
--- a/src/video/uikit/SDL_uikitappdelegate.m
+++ b/src/video/uikit/SDL_uikitappdelegate.m
@@ -64,9 +64,17 @@ int SDL_UIKitRunApp(int argc, char *argv[], SDL_main_func mainFunction)
     }
     forward_argv[i] = NULL;
 
-    /* Give over control to run loop, SDLUIKitDelegate will handle most things from here */
+    /* Give over control to the run loop. iOS 27 requires UIScene lifecycle adoption. */
     @autoreleasepool {
-        UIApplicationMain(argc, argv, nil, [SDLUIKitDelegate getAppDelegateClassName]);
+        NSString *delegateName = nil;
+
+        if (@available(iOS 13.0, tvOS 13.0, *)) {
+            delegateName = [SDLUIKitSceneDelegate getSceneDelegateClassName];
+        }
+        if (!delegateName) {
+            delegateName = [SDLUIKitDelegate getAppDelegateClassName];
+        }
+        UIApplicationMain(argc, argv, nil, delegateName);
     }
 
     /* free the memory we used to hold copies of argc and argv */
@@ -348,6 +356,96 @@ static UIImage *SDL_LoadLaunchImageNamed(NSString *name, int screenh)
 
 @end
 
+API_AVAILABLE(ios(13.0))
+@implementation SDLUIKitSceneDelegate
+
++ (NSString *)getSceneDelegateClassName
+{
+    return @"SDLUIKitSceneDelegate";
+}
+
+- (UISceneConfiguration *)application:(UIApplication *)application
+    configurationForConnectingSceneSession:(UISceneSession *)connectingSceneSession
+                                   options:(UISceneConnectionOptions *)options
+{
+    UISceneConfiguration *configuration =
+        [[UISceneConfiguration alloc] initWithName:@"SDLSceneConfiguration"
+                                       sessionRole:connectingSceneSession.role];
+    configuration.delegateClass = [SDLUIKitSceneDelegate class];
+    return configuration;
+}
+
+- (void)scene:(UIScene *)scene
+    willConnectToSession:(UISceneSession *)session
+                 options:(UISceneConnectionOptions *)connectionOptions
+{
+    if (![scene isKindOfClass:[UIWindowScene class]]) {
+        return;
+    }
+
+    ((UIWindowScene *)scene).delegate = self;
+
+    NSBundle *bundle = [NSBundle mainBundle];
+    [[NSFileManager defaultManager] changeCurrentDirectoryPath:[bundle resourcePath]];
+
+    SDL_AddHintCallback(SDL_HINT_IDLE_TIMER_DISABLED,
+                        SDL_IdleTimerDisabledChanged, NULL);
+
+    for (UIOpenURLContext *context in connectionOptions.URLContexts) {
+        [self sendDropFileForURL:context.URL];
+    }
+
+    SDL_SetMainReady();
+    [self performSelector:@selector(postFinishLaunch) withObject:nil afterDelay:0.0];
+}
+
+- (void)postFinishLaunch
+{
+    SDL_iPhoneSetEventPump(SDL_TRUE);
+    exit_status = forward_main(forward_argc, forward_argv);
+    SDL_iPhoneSetEventPump(SDL_FALSE);
+}
+
+- (void)sceneDidBecomeActive:(UIScene *)scene
+{
+    SDL_OnApplicationDidBecomeActive();
+}
+
+- (void)sceneWillResignActive:(UIScene *)scene
+{
+    SDL_OnApplicationWillResignActive();
+}
+
+- (void)sceneWillEnterForeground:(UIScene *)scene
+{
+    SDL_OnApplicationWillEnterForeground();
+}
+
+- (void)sceneDidEnterBackground:(UIScene *)scene
+{
+    SDL_OnApplicationDidEnterBackground();
+}
+
+- (void)sendDropFileForURL:(NSURL *)url
+{
+    NSURL *fileURL = url.filePathURL;
+    if (fileURL != nil) {
+        SDL_SendDropFile(NULL, fileURL.path.UTF8String);
+    } else {
+        SDL_SendDropFile(NULL, url.absoluteString.UTF8String);
+    }
+    SDL_SendDropComplete(NULL);
+}
+
+- (void)scene:(UIScene *)scene openURLContexts:(NSSet<UIOpenURLContext *> *)URLContexts
+{
+    for (UIOpenURLContext *context in URLContexts) {
+        [self sendDropFileForURL:context.URL];
+    }
+}
+
+@end
+
 @implementation SDLUIKitDelegate {
     UIWindow *launchWindow;
 }
diff --git a/src/video/uikit/SDL_uikitmessagebox.m b/src/video/uikit/SDL_uikitmessagebox.m
index 6ca7937a2..b41ec747e 100644
--- a/src/video/uikit/SDL_uikitmessagebox.m
+++ b/src/video/uikit/SDL_uikitmessagebox.m
@@ -83,7 +83,22 @@ static BOOL UIKit_ShowMessageBoxAlertController(const SDL_MessageBoxData *message
     }
 
     if (window == nil || window.rootViewController == nil) {
-        alertwindow = [[UIWindow alloc] initWithFrame:[UIScreen mainScreen].bounds];
+        if (@available(iOS 13.0, tvOS 13.0, *)) {
+            for (UIScene *scene in [UIApplication sharedApplication].connectedScenes) {
+                if ([scene isKindOfClass:[UIWindowScene class]] &&
+                    scene.activationState != UISceneActivationStateUnattached) {
+                    UIWindowScene *windowScene = (UIWindowScene *)scene;
+                    alertwindow = [[UIWindow alloc] initWithWindowScene:windowScene];
+                    alertwindow.frame = windowScene.coordinateSpace.bounds;
+                    break;
+                }
+            }
+        }
+
+        if (alertwindow == nil) {
+            alertwindow = [[UIWindow alloc] initWithFrame:[UIScreen mainScreen].bounds];
+        }
+
         alertwindow.rootViewController = [UIViewController new];
         alertwindow.windowLevel = UIWindowLevelAlert;
 
diff --git a/src/video/uikit/SDL_uikitwindow.m b/src/video/uikit/SDL_uikitwindow.m
index 5e99b06cd..a341bf9f3 100644
--- a/src/video/uikit/SDL_uikitwindow.m
+++ b/src/video/uikit/SDL_uikitwindow.m
@@ -149,6 +149,36 @@ static int SetupWindowData(_THIS, SDL_Window *window, UIWindow *uiwindow, SDL_boo
     return 0;
 }
 
+API_AVAILABLE(ios(13.0))
+static UIWindowScene *GetActiveWindowScene(void)
+{
+    if (@available(iOS 13.0, tvOS 13.0, *)) {
+        NSSet<UIScene *> *connectedScenes = [UIApplication sharedApplication].connectedScenes;
+
+        for (UIScene *scene in connectedScenes) {
+            if ([scene isKindOfClass:[UIWindowScene class]] &&
+                scene.activationState == UISceneActivationStateForegroundActive) {
+                return (UIWindowScene *)scene;
+            }
+        }
+
+        for (UIScene *scene in connectedScenes) {
+            if ([scene isKindOfClass:[UIWindowScene class]] &&
+                scene.activationState == UISceneActivationStateForegroundInactive) {
+                return (UIWindowScene *)scene;
+            }
+        }
+
+        for (UIScene *scene in connectedScenes) {
+            if ([scene isKindOfClass:[UIWindowScene class]]) {
+                return (UIWindowScene *)scene;
+            }
+        }
+    }
+
+    return nil;
+}
+
 int UIKit_CreateWindow(_THIS, SDL_Window *window)
 {
     @autoreleasepool {
@@ -205,9 +235,16 @@ int UIKit_CreateWindow(_THIS, SDL_Window *window)
         }
 #endif /* !TARGET_OS_TV */
 
-        /* ignore the size user requested, and make a fullscreen window */
-        /* !!! FIXME: can we have a smaller view? */
-        uiwindow = [[SDL_uikitwindow alloc] initWithFrame:data.uiscreen.bounds];
+        if (@available(iOS 13.0, tvOS 13.0, *)) {
+            UIWindowScene *windowScene = GetActiveWindowScene();
+            if (data.uiscreen == [UIScreen mainScreen] && windowScene != nil) {
+                uiwindow = [[SDL_uikitwindow alloc] initWithWindowScene:windowScene];
+            }
+        }
+        if (uiwindow == nil) {
+            /* Ignore the requested size and make a fullscreen legacy window. */
+            uiwindow = [[SDL_uikitwindow alloc] initWithFrame:data.uiscreen.bounds];
+        }
 
         /* put the window on an external display if appropriate. */
         if (data.uiscreen != [UIScreen mainScreen]) {
